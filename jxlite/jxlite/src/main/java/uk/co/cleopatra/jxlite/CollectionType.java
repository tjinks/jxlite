package uk.co.cleopatra.jxlite;

/**
 * The values of this enumeration identify the different types of collection
 * into which we can unmarshal an XML nodeset.
 */
enum CollectionType {
	NONE, LIST, ARRAY
}
